<div class="card comic-card">
    <img  src={{$image}} class="card-img-top" alt={{$title}}>
    
    <div class="card-body d-flex flex-column">
        <h5 class="card-title text-uppercase fw-bold">{{$title}}</h5>
        <p class="card-text">{{$saledate}}</p>
        
        <div class="mt-auto">
            <span class="badge bg-primary mb-2">{{$price}}</span>
            <a href="#" class="btn btn-outline-primary">Vedi dettagli</a>
        </div>
    </div>
</div>

